{
  "name": "fakebackend",
  "version": "1.0.0",
  "description": "a fake backend to serve some json data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server -p 3007 -w db.json"
  },
  "author": "Vlad",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
